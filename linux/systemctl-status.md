# Check Systemd Services States

```shell
systemctl status
```

`State: degraded` will show if some of the unit files are not starting properly.

To check the exact service run

```shell
systemctl
```
