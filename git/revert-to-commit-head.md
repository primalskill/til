# Revert To Commit on HEAD

Reverting to a commit ID on HEAD.

```shell
git reset --hard <commit ID>
```

------

You also need to force the update on the `origin` too.

```shell
git push origin -f
```
